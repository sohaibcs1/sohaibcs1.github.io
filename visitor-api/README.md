# Visitor API

Cloudflare Worker and D1 backend for the privacy-safe visitor map. It stores a daily salted hash instead of a raw IP address and exposes aggregate public statistics.

All commands are run from the repository root. See the main `README.md`.

